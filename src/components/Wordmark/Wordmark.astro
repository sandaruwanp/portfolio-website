---
---

<span class="wordmark">
    <span class="wordmark-name">Sandaruwan Pathirana</span>
    <span class="wordmark-rule" aria-hidden="true"></span>
</span>

<style>
    .wordmark {
        display: inline-flex;
        flex-direction: column;
        align-items: flex-start;
        gap: var(--space-xs);
    }

    .wordmark-name {
        font-family: var(--serif);
        font-size: 1.0625rem;
        font-weight: 600;
        line-height: 1;
        color: inherit;
        white-space: nowrap;
    }

    .wordmark-rule {
        width: 22px;
        height: 1px;
        background: var(--accent-gold);
    }
</style>
